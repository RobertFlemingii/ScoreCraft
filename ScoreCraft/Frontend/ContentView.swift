import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Button("Create Score") {
                // Handle create score button tap
            }
            .padding()
            
            Button("Open Score") {
                // Handle open score button tap
            }
            .padding()
        }
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
